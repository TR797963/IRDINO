# Model Zoo

This repository does not include pretrained weights or trained checkpoints.

If weights are released later, download links can be added here.

Recommended local paths:

```text
weights/
checkpoints/
```

`weights/` and `checkpoints/` are excluded by `.gitignore` and should not be committed.
